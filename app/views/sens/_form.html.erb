<%= form_for(@sen) do |f| %>
  <% if @sen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sen.errors.count, "error") %> prohibited this sen from being saved:</h2>

      <ul>
      <% @sen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sen_code %><br>
    <%= f.text_field :sen_code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
