<%= form_for(@assessment_evidence) do |f| %>
  <% if @assessment_evidence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assessment_evidence.errors.count, "error") %> prohibited this assessment_evidence from being saved:</h2>

      <ul>
      <% @assessment_evidence.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :evidence_file %><br>
    <%= f.text_field :evidence_file %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
